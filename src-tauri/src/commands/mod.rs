// NexusLog Commands — Tauri IPC handlers
//
// These functions are exposed to the frontend via `invoke()`.
// Each command is registered in lib.rs via `tauri::generate_handler![]`.

pub mod evtx;

// Removed greet command

