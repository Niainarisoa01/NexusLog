// NexusLog UI Components
//
// This directory will contain reusable UI components:
// - EventTable.tsx (Phase 1) — Virtualized event table
// - FilterBar.tsx (Phase 1) — Filter controls
// - DetailPanel.tsx (Phase 1) — Event detail panel with tabs
// - FileDropZone.tsx (Phase 1) — Drag & drop file loading
// - ChannelSidebar.tsx (Phase 1) — Sidebar with files/channels
// - SearchBar.tsx (Phase 2) — Full-text search
// - Dashboard.tsx (Phase 3) — Statistics dashboard
// - SigmaPanel.tsx (Phase 3) — Sigma detection results
// - XPathEditor.tsx (Phase 3) — Monaco-based XPath editor
// - AlertManager.tsx (Phase 3) — Alert rules CRUD
// - Timeline.tsx (Phase 3) — Multi-file correlation timeline

export {};
