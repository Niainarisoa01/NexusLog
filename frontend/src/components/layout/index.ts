// NexusLog Layout Components
//
// This directory will contain the app layout:
// - AppLayout.tsx (Phase 1) — Main app layout with sidebar, header, content area
// - Header.tsx (Phase 1) — Top navigation bar
// - Sidebar.tsx (Phase 1) — Left sidebar with channels/files

export {};
